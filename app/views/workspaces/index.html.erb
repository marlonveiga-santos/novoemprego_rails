<h1>Suas Vagas</h1>
<% @jobs.each do |job| %>
  <h2> <%= job.title %> </h2>
  <h3> <%= job.description %> </h3>
  <%= link_to 'Delete', job_path(job.id),  method: :delete,
  data: { confirm: 'Confirmar exclusão?' }, :class => "card-link" %>
<% end %>